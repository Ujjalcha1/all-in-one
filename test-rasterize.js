const fs = require('fs');

async function testRasterize() {
  // We can't easily run pdfjs-dist and canvas in Node without canvas package
  console.log("Will implement in browser");
}

testRasterize();
